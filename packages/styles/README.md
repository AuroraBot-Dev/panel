# @vben/styles

全局样式（设计令牌、基础 CSS 与 scss-bem 工具）及各 UI 库适配样式。业务上有通用的样式文件可以放在这里。

## 用法

### 添加依赖

```bash
# 进入目标应用目录，例如 apps/web
# cd apps/web
pnpm add @vben/styles
```

### 使用

```ts
import '@vben/styles';
import '@vben/styles/naive';
```

name: Deploy AuroraBot Dashboard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  not-configured:
    name: Deployment not configured
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment boundary
        run: echo "Production deployment is intentionally not configured in phase one."
